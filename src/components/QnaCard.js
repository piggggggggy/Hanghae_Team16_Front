import React from "react";


const QnaCard = (props) => {

    return (
        <React.Fragment>
            text(제목)
            text(날짜)
            text(내용)
            text(이름)
            text(댓글 수)
            button(버튼)


        </React.Fragment>
    )
}

export default QnaCard;