import React from "react";


const MyComment = (props) => {

    return (
        <React.Fragment>
            text
            btn
            grid 
                text(내용)
                text(날짜)
            
        </React.Fragment>
    )
}

export default MyComment;