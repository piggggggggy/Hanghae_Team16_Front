import React from "react";


const StudyCard = (props) => {

    return (
        <React.Fragment>
            text(level)
            text(날짜)
            text(제목)
            text(내용)
            text(인원)
            text(카테고리)
            button
            button
            
        </React.Fragment>
    )
}

export default StudyCard;